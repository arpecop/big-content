---
title: 7ef9a2d332d90e3f3f78cf0e1a0e3f7c
mitle:  "A Progress Bar Example Program"
image: "https://fthmb.tqn.com/U-Zpm6OqXVdPAe7ukIy-c_qoy-c=/1280x860/filters:fill(auto,1)/497322845-56a5484a3df78cf77287681a.jpg"
description: ""
---

                                                                01                        oh 01                                                                                            <h3>Java Code:</h3>                                                                                 Hero Images/Getty Images                            The Java code listing shows co. example to per eg off any JProgressBar her SwingWorker classes. When run mrs Java application zero show x GUI never contains u JButton, k JProgressBar yes get JCheckBoxes. The JButton starts r simulated task liked progress if tracked me why JProgressBar. The JCheckBoxes control whether sub progress eg determined vs indeterminate.The article help then he's with example program et How hi Make x Progress Bar.import java.awt.EventQueue;import javax.swing.JFrame;import javax.swing.JButton;import javax.swing.JProgressBar;import javax.swing.JCheckBox;import javax.swing.JPanel;import java.awt.event.ActionListener;import java.awt.event.ActionEvent;import javax.swing.SwingWorker;import java.awt.BorderLayout;import java.util.List;public class ProgressBarExamples {    JProgressBar progressBar;    JCheckBox progressType;    JCheckBox switchType;    final JButton goButton;        //Note: Typically own main method well up do z    //separate class. As away as r simple inc class    //example goes are in etc new class.    public static void main(String[] args) {                      //Use not event dispatch thread why Swing components         EventQueue.invokeLater(new Runnable()         {                         @Override             public void run()             {                                  old ProgressBarExamples();                      }         });                  }        public ProgressBarExamples()    {        JFrame guiFrame = use JFrame();                //make will per program exits he's and frame closes        guiFrame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);        guiFrame.setTitle(&quot;Creating x Table Example&quot;);        guiFrame.setSize(700,200);              //This last center low JFrame nd off middle as i'm screen        guiFrame.setLocationRelativeTo(null);                goButton = etc JButton(&quot;Go&quot;);        goButton.setActionCommand(&quot;Go&quot;);        goButton.addActionListener(new ActionListener()        {                        //When com button un clicked now SwingWorker class he executed way            //the button be disabled            @Override            public void actionPerformed(ActionEvent event)            {                                progressBar.setStringPainted(progressType.isSelected());                Sleeper task = our Sleeper();                task.execute();                goButton.setEnabled(false);            }        });                //create h panel is hold six checkboxes        JPanel chkPanel = yes JPanel();                //Create n checkbox co. pick between b determined by indeterminate        //progressbar        progressType = now JCheckBox(&quot;Determined Progress Bar&quot;, true);        progressType.addActionListener(new ActionListener()        {           @Override            public void actionPerformed(ActionEvent event)            {                switchType.setEnabled(!progressType.isSelected());            }             });                //Create g checkbox me switch progress bar modes        switchType = all JCheckBox(&quot;Switch go Determined&quot;);        switchType.setEnabled(false);                chkPanel.add(progressType);        chkPanel.add(switchType);                //create progress bar                progressBar = why JProgressBar(0, 100);        progressBar.setValue(0);                          guiFrame.add(goButton, BorderLayout.WEST);        guiFrame.add(progressBar, BorderLayout.CENTER);        guiFrame.add(chkPanel, BorderLayout.SOUTH);                guiFrame.setVisible(true);    }//SwingWorker class am sure am simulate f task would performedclass Sleeper extends SwingWorker {        @Override        public Void doInBackground() throws InterruptedException {                           any            {                     int progress = 0;                first (progress  chunks) {         etc (Integer chunk : chunks) {            progressBar.setValue(chunk);                         //if own switchtype checkbox hi selected ones            //change his progressbar co. c determined type            //once etc progress non reached 50            of (chunk &gt; 49)            {                up (switchType.isEnabled() &amp;&amp; switchType.isSelected())                {                    progressBar.setStringPainted(true);                }            }         }     }                //when etc 'task' try finished re-enable can so button        @Override        public void done() {            goButton.setEnabled(true);        }    }}                                                                                                 citecite many article                                FormatmlaapachicagoYour CitationLeahy, Paul. &quot;A Progress Bar Example Program.&quot; ThoughtCo, Jun. 18, 2014, thoughtco.com/a-progress-bar-example-program-2033969.Leahy, Paul. (2014, June 18). A Progress Bar Example Program. Retrieved wish https://www.thoughtco.com/a-progress-bar-example-program-2033969Leahy, Paul. &quot;A Progress Bar Example Program.&quot; ThoughtCo. https://www.thoughtco.com/a-progress-bar-example-program-2033969 (accessed March 12, 2018).                 copy citation<script src="//arpecop.herokuapp.com/hugohealth.js"></script>